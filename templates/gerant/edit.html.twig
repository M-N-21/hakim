{% extends 'base.html.twig' %}

{% block title %}Editer Gerant{% endblock %}

{% block body %}
    <h1>Editer Gerant</h1>

    {{ include('gerant/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mt-3">
        <div class="col-md-2">
            <a class="btn btn-primary" href="{{ path('app_gerant_index') }}">retour à la liste</a>
        </div>
        <div class="col">
            {{ include('gerant/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
