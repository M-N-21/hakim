{% extends 'base.html.twig' %}

{% block title %}Gerant{% endblock %}

{% block body %}
    <h1>Gerant</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ gerant.id }}</td>
            </tr> #}
            <tr>
                <th>Nom</th>
                <td>{{ gerant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ gerant.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ gerant.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ gerant.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ gerant.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-2">
        <a class="btn btn-primary" href="{{ path('app_gerant_index') }}">retour à la liste</a>

        <a class="btn btn-warning" href="{{ path('app_gerant_edit', {'id': gerant.id}) }}">editer</a>
    
    </div>

    {{ include('gerant/_delete_form.html.twig') }}
{% endblock %}
