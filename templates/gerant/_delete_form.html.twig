<form method="post" action="{{ path('app_gerant_delete', {'id': gerant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerant.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
