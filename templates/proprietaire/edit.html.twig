{% extends 'base.html.twig' %}

{% block title %}Editer Proprietaire{% endblock %}

{% block body %}
    <h1>Editer Proprietaire</h1>

    {{ include('proprietaire/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mt-3">
        <div class="col-md-2">
            <a class="btn btn-primary" href="{{ path('app_proprietaire_index') }}">retour à la liste</a>
        </div>
        <div class="col">
            {{ include('proprietaire/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
