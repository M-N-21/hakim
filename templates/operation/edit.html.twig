{% extends 'base.html.twig' %}

{% block title %}Editer Operation{% endblock %}

{% block body %}
    <h1>Editer Operation</h1>

    {{ include('operation/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mt-3">
        <div class="col-md-2">
            <a class="btn btn-primary" href="{{ path('app_operation_index') }}">retour à la liste</a>
        </div>
        <div class="col">
            {{ include('operation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
