{% extends 'base.html.twig' %}

{% block title %}Editer Produit{% endblock %}

{% block body %}
    <h1>Editer Produit</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mt-3">
        <div class="col-md-2">
            <a class="btn btn-primary" href="{{ path('app_produit_index') }}">retour à la liste</a>
        </div>
        <div class="col">
            {{ include('produit/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
