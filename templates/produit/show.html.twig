{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr> #}
            <tr>
                <th>NomProduit</th>
                <td>{{ produit.nomProduit }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ produit.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-2">
        <a class="btn btn-primary" href="{{ path('app_produit_index') }}">retour à la liste</a>

        <a class="btn btn-warning" href="{{ path('app_produit_edit', {'id': produit.id}) }}">editer</a>
    
    </div>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
