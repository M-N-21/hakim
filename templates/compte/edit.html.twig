{% extends 'base.html.twig' %}

{% block title %}Editer Compte{% endblock %}

{% block body %}
    <h1>Editer Compte</h1>

    {{ include('compte/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mt-3">
        <div class="col-md-2">
            <a class="btn btn-primary" href="{{ path('app_compte_index') }}">retour à la liste</a>
        </div>
        <div class="col">
            {{ include('compte/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
