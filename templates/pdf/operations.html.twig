<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Operations</title>
</head>
<body>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 20px;
    }

    h1 {
        text-align: center;
        color: green; /* Couleur du texte */
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
    }

    th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
    }

    th {
        background-color: #f2f2f2; /* Couleur de fond des cellules d'en-tête */
    }

    td {
        background-color: #fff; /* Couleur de fond des cellules de données */
    }

    tr:nth-child(even) {
        background-color: #f9f9f9; /* Couleur de fond des lignes paires */
    }

    tr:hover {
        background-color: #e1e1e1; /* Couleur de fond lorsqu'on survole une ligne */
    }

    td[colspan="8"] {
        font-style: italic;
        text-align: center;
    }
</style>

    <h1 align="center">Etat Global des Operations</h1>
    <table class="table table-responsive-md">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Libelle</th>
                <th>Date</th>
                <th>Montant Debit</th>
                <th>Montant Credit</th>
                <th>Compte</th>
                {# <th>Date</th> #}
            </tr>
        </thead>
        <tbody>
        {% for o in operations %}
            <tr>
                {# <td>{{ produit.id }}</td> #}
                <td>{{ o.libelleOperation }}</td>
                <td>{{ o.dateOperation|date('d/m/Y')? : '' }}</td>
                <td>{{ operation.montantDebit }}</td>
                <td>{{ operation.montantCredit }}</td>
                <td>{{ operation.compte }}</td>
                
                {# <td>{{ produit.date ? produit.date|date('Y-m-d') : '' }}</td> #}
            </tr>
        {% else %}
            <tr>
                <td style="color: green;" colspan="8">Aucune Operation</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</body>
</html>