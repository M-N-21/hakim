{% extends 'base.html.twig' %}

{% block title %}Editer Depense{% endblock %}

{% block body %}
    <h1>Editer Depense</h1>

    {{ include('depense/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mt-3">
        <div class="col-md-2">
            <a class="btn btn-primary" href="{{ path('app_depense_index') }}">retour à la liste</a>
        </div>
        <div class="col">
            {{ include('depense/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
